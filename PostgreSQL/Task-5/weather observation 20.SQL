SELECT ROUND(AVG(LAT_N),4) 
FROM ( SELECT LAT_N, ROW_NUMBER() 
OVER (ORDER BY LAT_N) AS ROW_NUM, COUNT(*)
OVER () AS TOTAL_ROW FROM STATION ) AS SUB WHERE ROW_NUM IN ((TOTAL_ROW+1)/2,(TOTAL_ROW+2)/2);